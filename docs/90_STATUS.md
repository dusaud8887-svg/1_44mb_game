# 90 — 구현 상태 (STATUS 정본)

**스펙 문서(00~50)는 상태를 말하지 않고, 이 문서만 상태를 말한다.** V1에서 SPEC/STATUS 혼합이 만든 드리프트(문서 줄 수·카드 수·구현 선언 불일치 등, 원문 §5.2)의 재발 방지 장치다. 수치·규칙 변경 커밋은 이 문서와 [CHANGELOG.md](CHANGELOG.md)를 함께 갱신한다.

```yaml
status: verified
verified_commit: 25b3bb1
verified_at: 2026-07-16
verified_by: 정적 코드 대조 (이 세션은 리눅스 — MSVC 빌드·실행 검증 불가)
```

## 1. 큰 그림

| 항목 | 상태 |
|---|---|
| 코드가 구현하는 게임 | **V1 "LAST LIVE"** (자동 탄창·FORMAT/0·단일 SIGNAL) — 스펙은 [archive/v1-last-live/](archive/v1-last-live/) |
| 문서가 기술하는 게임 | **V2** (수동 편성·노아·3색 링) — 00~50 전체 |
| V2 구현 | **미착수.** 경로 결정은 go/no-go 게이트([50](50_PRODUCTION.md) §1) |
| 소스 | `src/echo144.c` 단일 파일 **2,050줄** (구 문서의 "1,174줄"은 구식 — 드리프트 사례) |
| 릴리스 exe | 마지막 실측 135,168B (2026-07-14, 이후 아트 반영으로 재측정 필요 — 리눅스 세션에서 불가) |
| 카드 수 | 실제 16종 (구 아트 문서의 "19종"은 컷 이전 계획치) |
| selftest | 2026-07-15 시점 전부 PASS (V1 규칙 기준). 최악 충돌 틱 실측 0.162ms (2026-07-14) — 성능 수치의 정본은 이 문서 |
| 자산 | V1: 인물 11종 + 글리프 7종 + 아이콘 16종 = 2,952B 배선 완료. **V2 정책: DNA만 계승, 전면 재설계**(ADR-0008) — V1 자산은 no-go 시 그대로 유효 |

V1의 초기 정적 평가(콘셉트 9/10, 런타임 정확성 신뢰도 4.5/10, 기능 완성도 ≈80%·제출 준비도 ≈55~65% — 이동·스케줄러·GDI 수정 전 기준)는 [archive/ECHO144_complete_design_history.md](archive/ECHO144_complete_design_history.md) 부록 A에 동결. V1 자산의 검수 상태 분리 표기 원칙(단품 검수 PASS / Windows 실빌드 검수 PENDING / 외부 테스터 검수 PENDING)은 아래 §2에 반영되어 있다.

## 2. V1 잔여 작업 (no-go 시 재개할 것)

- Windows 실빌드 스크린샷 검수 (MSVC 필요 — G-게이트 수동 항목).
- 엘리트 피격 프레임 전용 트리거.
- G1/G2 사람 테스트 미실시 (테스터 미모집 상태).
- 음악 모티프의 음계화 마감 (근사 구현 상태).

## 3. 검증된 P0/P1 결함 — 코드 라인 대조 (2026-07-16)

원문 2부의 주장을 `25b3bb1`과 대조. **전부 실재.** 수정 계획은 [30_TECH.md](30_TECH.md) §3, 일정은 [50](50_PRODUCTION.md) W-FIX.

| # | 결함 | 위치 (echo144.c) | 상태 |
|---|---|---|---|
| 1 | 이동 정수 절삭 — 방향 비대칭 (플레이어 0.9px/틱이 절삭돼 우·하 이동 정지, 좌·상 과속. 적 동일) | `:734` `(float)clampi((int)g.px,…)`, `:755`, `:403`, `:569` | **미수정** |
| 2 | 카드 스케줄러 — 손패 교체 15틱이 기본 30틱을 가산이 아니라 **대체**, `>0`-감소 패턴으로 30→실제 31틱 | `:598`, `:637-656`, `:830` | **미수정** (B-공식·selftest 기준선과 불일치) |
| 3 | GDI 텍스트 색 R/B 반전 — DIB 상수 `0x00RRGGBB`를 COLORREF로 직접 캐스팅 | `:1568` | **미수정** |
| 4 | 비정수 배율 — 클라이언트 전체 StretchBlt, 레터박스 없음 (창 리사이즈 시 픽셀 비율 파괴) | `:1857` (창은 WS_OVERLAPPEDWINDOW 리사이즈 가능, `:1886`) | **미수정** |
| 5 | 손패 경계 modifier·거짓 LINK·MACRO 내림 반올림 | `card_now/execute_card` 경로 | **미수정** (정적 확인 부분적 — 테스트로 확정 필요) |
| 6 | 강제 상태 전환 후 같은 틱 로직 지속 | 상태 머신 | **미확정** (전환 경로별 return 감사 필요) |
| 7 | spawn 실패에도 예산 소모 / TROJAN 순회 중 분열 삽입 / 상자 재배치 / 초기 SIGNAL burst 경로 | spawn/update 경로 | **미확정** (감사 필요) |
| 8 | GulimChe 시스템 폰트 의존 | `:1896` | 알려진 리스크 (W6 게이트 or 내장 폰트) |

부분 완화 확인: `SetBkMode(TRANSPARENT)` 존재(`:1790`), `draw_sprite_2bpp` stride 버그는 2026-07-15 수정 완료.

주의: #1·#2는 **밸런스가 버그 위에 튜닝**되어 있을 가능성을 뜻한다 — 수정 시 적 실효 속도·발동 밀도가 변하므로 B-값 재튜닝을 동반한다([50](50_PRODUCTION.md) 리스크 8).

## 4. V2 구현 체크리스트 (착수 시 갱신)

| 블록 | 스펙 | 상태 |
|---|---|---|
| W-FIX 정확성 수정 + 대칭/타임라인 테스트 | [30](30_TECH.md) §3 | 미착수 |
| EDIT/ON AIR/BREAK/CLEANUP 턴 구조 | [10](10_MECHANICS.md) §2~7 | 미착수 |
| CUE·SEEK(신)·카드 8장 | [15](15_CARDS.md), B2-카드 | 미착수 |
| 적 의도 4종 + 예고 HUD | [10](10_MECHANICS.md) §5 | 미착수 |
| 64칸 **논리** 링(+채팅 질감) — P0 표시는 16대구간 임시 렌더러 | [10](10_MECHANICS.md) §8 | 미착수 |
| OPEN CHANNEL + 반전 + 위상 | [10](10_MECHANICS.md) §10 | 미착수 |
| TREND MIRROR 1종 | [10](10_MECHANICS.md) §9 | 미착수 |
| 전략 봇·SIM 확장 | [20](20_BALANCE.md) §SIM | 미착수 |
| balance.def·콘텐츠 컴파일러·이벤트·구조 분리 | [30](30_TECH.md) §4~7 | 미착수 |
| 내장 비트맵 폰트 | [30](30_TECH.md) §9 | 미착수 |
| 저장 파일 | [30](30_TECH.md) §8 | 미착수 |
| V2 자산 재설계 (DNA 계승) | [41](41_PIXEL_ART.md) §1~§2·§7 | **1차 제작 완료** (2026-07-17): 초상 64×64 3종 + 전장체 3종 + 메인 키아트 192×108 → `assets/px_v2/` (원본 `tools/v2/*.py`, [CHANGELOG 6차](CHANGELOG.md)). 남음: 표정 델타·애니 프레임·카드 아이콘 24종·적 글리프·Aseprite 이관·코드 배선 |

## 5. 문서-코드 동기화 규칙

1. 스펙 문서에 "구현 ✅" 금지 — 상태는 여기와 CHANGELOG에만.
2. 이 문서의 `verified_commit`이 현재 HEAD와 멀어지면(±10 커밋) 재검증하고 스탬프를 갱신한다.
3. 수치는 [20](20_BALANCE.md) → `balance.def` → 코드 순서의 단일 원천을 지킨다. 코드에서 먼저 바꾸는 것을 금지.
